# UAWSO required environment variables. Copy to a real .env (untracked) or
# set these in the scheduler's environment. Never commit real values.
# No default/sample value is provided here - operators must supply real ones.

PGHOST=
PGPORT=
PGDATABASE=
PGUSER=
PGPASSWORD=
